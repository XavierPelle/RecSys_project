<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Formulaire Recommandation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f8f9fa;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        position: relative;
        overflow: hidden;
      }

      .circle {
        position: absolute;
        border-radius: 50%;
        opacity: 0.3;
        z-index: -1;
      }

      .circle.one {
        width: 150px;
        height: 150px;
        background: #b0c4de;
        top: 10%;
        left: 10%;
      }

      .circle.two {
        width: 200px;
        height: 200px;
        background: #d8bfd8;
        bottom: 15%;
        right: 10%;
      }

      .circle.three {
        width: 120px;
        height: 120px;
        background: #87cefa;
        bottom: 50%;
        left: 30%;
      }

      .form-container {
        background: rgba(227, 231, 241, 0.95);
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        width: 300px;
        text-align: center;
        position: relative;
        z-index: 10;
      }

      h2 {
        color: #6a5acd;
        margin-bottom: 15px;
      }

      label {
        display: block;
        font-size: 14px;
        margin: 10px 0 5px;
        color: #4a4a4a;
      }

      input {
        width: 100%;
        padding: 8px;
        border: 1px solid #b0c4de;
        border-radius: 6px;
        font-size: 14px;
        background: #ffffff;
      }

      input[type="submit"] {
        background: #6a5acd;
        color: white;
        border: none;
        cursor: pointer;
        margin-top: 15px;
        padding: 10px;
        transition: 0.3s;
      }

      input[type="submit"]:hover {
        background: #483d8b;
      }

      input:focus {
        outline: none;
        border-color: #6a5acd;
        box-shadow: 0px 0px 5px rgba(106, 90, 205, 0.5);
      }
    </style>
  </head>
  <body>
    <div class="circle one"></div>
    <div class="circle two"></div>
    <div class="circle three"></div>

    <div class="form-container">
      <h2>Obtenir des recommandations</h2>
      <form id="recommendForm">
        <label for="productId">ID du produit :</label>
        <input
          type="text"
          id="productId"
          name="productId"
          required
          minlength="1"
          maxlength="8"
        />

        <label for="count">Nombre de suggestions :</label>
        <input
          type="number"
          id="count"
          name="count"
          required
          min="1"
          max="100"
        />

        <input type="submit" id="btn" value="Obtenir recommandations" />
      </form>
    </div>
  </body>
  <script>
    document
      .getElementById("recommendForm")
      .addEventListener("submit", function (event) {
        event.preventDefault();

        let productId = document.getElementById("productId").value;
        let count = document.getElementById("count").value;

        // Change l'URL et redirige vers la requête GET
        window.location.href = `/suggest/${productId}/${count}`;
      });
  </script>
</html>
